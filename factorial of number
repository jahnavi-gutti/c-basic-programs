#include <stdio.h>
int num;
int factorial(int n)
{
	if (n == 0)
		return 1;
	return n * factorial(n - 1);
}
int main()
{ 
	scanf("%d",&num);
	printf("Factorial of %d is %d",num, factorial(num));
	return 0;
}
/*
5

Factorial of 5 is 120

*/
